os: linux

vm:
  size: [large]
language: java
services:
  - docker
cache: bundler
dist: bionic

install: skip

jobs:
  include:
    - name: failing
      script:
        - echo "Starting sleep loop..."
        - for i in {1..5}; do
            echo "Iteration $i";
            sleep 60;
          done
        - echo "Sleep loop completed."
        - true

after_success:
  - echo done
  - echo $TRAVIS_TEST_RESULT
after_failure:
  - echo failed
  - echo $TRAVIS_TEST_RESULT


 

  
  
  
  


      
      



      

      






  


